<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title><%=user.title%></title>
	</head>
	<body>
		<h1>Welcome to the <%= user.name %></h1>
		<h1>Welcome to the <%= user.age %></h1>
		<h1>Welcome to the <%= user.email %></h1>
		<ul>
			<% user.skills.forEach((item)=>{ %>
			<li><%= item %></li>
			<% }) %>
		</ul>
	</body>
</html>
